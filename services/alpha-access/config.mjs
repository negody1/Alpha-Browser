/** Shared Alpha Browser release info (same repo as desktop app-config.ts). */
export const GITHUB_REPO = 'negody1/Alpha-Browser';

export function githubReleasesUrl() {
  return `https://github.com/${GITHUB_REPO}/releases`;
}
